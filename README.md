
- Frontend: JavaScript (placeholder)
- Backend: Python (placeholder)

OUR STRUCTURE: 
- frontend/ — JS placeholder
- backend/  — Python placeholder
- docs/     — notes, design, API drafts

WORKFLOW:
- Default branch: main
- Use feature branches: feat/*, fix/*, chore/*
- Merge via Pull Requests to main

FLEXING:
- Allowed

DEPENDENCIES:
- MongoDB Community Edition (https://www.mongodb.com/try/download/community)
    - Recommend installing compass (MongoDB GUI) for debugging, enabled by default with the above msi installer
    - Alternatively use mongosh for debugging instead

MODULES:
pip install pymongo
pip install python-dotenv
pip install pytest-asyncio